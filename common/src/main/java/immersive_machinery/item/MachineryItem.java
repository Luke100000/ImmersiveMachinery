package immersive_machinery.item;

import immersive_aircraft.item.VehicleItem;

public class MachineryItem extends VehicleItem {
    public MachineryItem(Properties settings, VehicleConstructor constructor) {
        super(settings, constructor);

        // todo place on full blocks only
    }
}
